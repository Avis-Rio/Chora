# T22 端到端验收报告

**日期**: 2026-07-12
**fixture**: `content_archive/2026-05-13/youtube_硅谷101_Token经济学：AI时代的新货币战争`
**状态**: ✅ 端到端 Pipeline 全部跑通

## 4 步执行结果

### Step A: `export_to_json.py --all`
- ✅ 扫描 content_archive 全部 44 个目录
- ✅ 输出 `content_export.json`（~2.97 MB）
- ✅ 字段结构完整（id/title/source_url/platform/channel/publish_date/cover_path/summary/reading_time/score/tags/quotes/book_list/rewritten/transcript/word_count/guests/folder_path/exported_at）

### Step B: `generate_frontend_data.py`
- ✅ 同步 `frontend/data/content.json` + `frontend/public/data/content.json`（各 44 items / 397406 bytes）
- ✅ 同步 `summary.json`（total=44, youtube=30, podcast=14, 34 干净英文 tags）
- ✅ 无反引号脏标签

### Step C: `generate_distribution.py` (Guizang renderer)
- ✅ `--platform all --renderer guizang --guizang-mode auto --image-assets plan` 成功
- ✅ 生成 14 个产物文件：
  - 8 个 JSON (source/insights/visual_system/visual_briefs/card_copy/image_assets/philosophical_epilogue/manifest)
  - 2 个 HTML (xhs/index.html 1451 行 + wechat/index.html 1035 行)
  - 2 个 post.md + render.cjs
  - 1 个 wechat/appendix.md
- ✅ Chromium 真跑（不带 `--no-export-images`）：生成 3 wechat PNG + 11 xhs PNG + 11 小尺寸缩略图
- ⚠️ **validator 报 17 个 fail**（R0 rule）—— pipeline 工作正常，质量门真实发 fail，是有效信号不是工具崩了

### Step D: `utils/content_validator.py --days 0 --fix --report`
- ✅ 扫描 44 个目录，**44 完整 / 0 不完整**
- 注：默认 `--days 30` 会错过 fixture（2026-05-13 距今 60 天）；用 `--days 0` 显式选择"全部"

## 发现

1. **distribution_pipeline 真实完整工作**——从 metadata.md → insights → visual_system → renderer → chromium PNG 一气呵成
2. **`--fail-on-quality-gate` 真能 fail**——证明 quality gate 不是装饰品，捕获真实 17 个 R0 rule 违反
3. **`content_validator.py --days 0` 是必要的**——默认值 30 天会漏掉所有 fixture
4. **`utils/content_validator.py` import 仍用 sys.path hack**：CI 跑需 `PYTHONPATH=.`

## 残留 TODO（如要进入生产）

- 17 个 R0 violation 需要人工 review（fix 文案 / 调整 layout）
- 跑全套 production：用真实 content（不是 fixture），通过端到端 automation
- 加 Playwright browser install 到 CI：当前 chromium 在 `.ms-playwright/` 但 CI 没装
